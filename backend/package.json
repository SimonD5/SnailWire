{
  "name": "snailwire-backend",
  "version": "0.1.0",
  "private": true,
  "description": "API du site SnailWire — déployée sur Render.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node --watch server.js",
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2"
  }
}

