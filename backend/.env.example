# Copie ce fichier en .env pour du dev local si besoin.
PORT=3001
# URL du frontend deploye sur Vercel (pour restreindre le CORS en prod).
# En local, laisse vide : tout est autorise.
FRONTEND_ORIGIN=
